<% if current_user&& current_user[:role]>= SCHOLAR %>
  <div id="tagForm" style="display:none;">
    <%= form_for [@taglinkable, @taglink] do |f| %>
      <% if @taglink.errors.any? %>
        <div class="error_messages">
          <h2>Please correct the following errors.</h2>
          <ul>
          <% @taglink.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
      <p>
        <%= f.select :tag_id, @tags.map{ |c| [c.name, c.id] } %>
      </p>
      <div class="actions">
        <%= f.submit '添加此标签' %>
      </div>
    <% end %>
  </div>
<% end %>