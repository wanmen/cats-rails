<main class="container" role="main">
  <article class="col-sm-8">
    <section class="wm-book-gallery">
      <h3 class="page-header">
        <span class="glyphicon glyphicon-star"></span> 最新书单
        <a href="">查看更多</a>
      </h3>
      <div class="row">
        <% @newestBooklist.each do |booklist| %>
          <figure class="col-sm-4">
            <a href="/lists/<%=booklist.id %>" class="thumbnail">
               <img src="<%=booklist.links[0].linkable.cover %>!big" alt="">
            </a>
            <figcaption>
              <a href="/lists/<%=booklist.id %>"><%=booklist.title%></a>
            </figcaption>
          </figure>
        <% end %>
      </div>
    </section>

    <section class="wm-book-gallery">
      <h3 class="page-header">
        <span class="glyphicon glyphicon-star"></span> 热门书单
        <a href="">查看更多</a>
      </h3>
      <div class="row">
	<% count=0 %>
        <% @popularBooklist.each do |booklist| %>
	<% count = count+1 %>
	<% if count = 4 %>
	     </div><div class="row">
	<% end %>
          <figure class="col-sm-4">
            <a href="/lists/<%=booklist.id %>" class="thumbnail">
               <img src="<%=booklist.links[0].linkable.cover %>!big" alt="">
            </a>
            <figcaption>
              <a href="/lists/<%=booklist.id %>"><%=booklist.title%></a>
            </figcaption>
          </figure>
        <% end %>
      </div>
    </section>

    <section class="wm-book-gallery">
      <h3 class="page-header">
        <span class="glyphicon glyphicon-calendar"></span> 最新图书
        <a href="">查看更多</a>
      </h3>
      <div class="row">
        <% @newest.each do |book| %>
          <figure class="col-sm-4">
            <a class="thumbnail" href="/books/<%=book.id %>">
              <img src="<%=book.cover %>!big" alt="">
            </a>
            <figcaption>
              <a href="/books/<%=book.id %>"><%=book.title %></a>
            </figcaption>
          </figure>
        <% end %>
      </div>
    </section>
  </article>

  <aside class="col-sm-4">

    <section>
      <h3 class="page-header">
        <span class="glyphicon glyphicon-book"></span> 热门标签
      </h3>
      <ol class="wm-list-popular">
       <% @popularTags.each do |tag| %>
        <li><a href="/tags/<%=tag.id %>"> <%=tag.name %></a></li>
       <% end %>
      </ol>
    </section>

    <section>
      <h3 class="page-header">
        <span class="glyphicon glyphicon-book"></span> 热门图书
      </h3>
      <ol class="wm-list-popular">
       <% @popular.each do |book| %>
        <li><a href="/books/<%=book.id %>"> <%=book.title %></a></li>
       <% end %>
      </ol>
    </section>

    <section>
      <h3 class="page-header">
        <span class="glyphicon glyphicon-tasks"></span> 热门书单
      </h3>
      <ol class="wm-list-popular">
       <% @popularBooklist.each do |list| %>
         <li><a href="/lists/<%=list.id %>"><%=list.title %></a></li>
       <% end %>
      </ol>
    </section>
  </aside>
</main>
