<%= @rates.length %>rates
<div id="rates">
<% @rates.each do |rate| %>
  <div class="rate">
  	<em><%= rate.user[:name]%> on <%= rate.created_at.strftime('%b %d, %Y at %H:%M') %></em>
    <%= rate.star%>stars
  </div>
<% end %>
</div>