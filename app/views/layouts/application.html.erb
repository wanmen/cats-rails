<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <meta name="keywords" content="网络大学，高等教育"/>
    <meta name="description" content="万门大学不仅是一所网络大学，它也是一种信仰，这种信仰来自于我们相信每个人内在的价值，相信大学之道的存在；它更是一次中国高等教育改革的践行——在符合时代特征和发展规律的基础上，对大学精神以及大学教育进行深入的思考和尝试。"/>
    <title>万门大学</title>
    <link rel="shortcut icon" href="/assets/favicon.ico">
    <!-- Bootstrap core CSS -->
    <link href="/assets/bootstrap.css" rel="stylesheet">

    <link href="/assets/wm-default.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="js/html5shiv.js"></script>
      <script src="js/respond.min.js"></script>
    <![endif]-->
    <style>
    #videoPanel{
      z-index: 100;
      width:100%;
      margin-top: 80px;
      padding:0 100px;
      position: absolute;
      top:0px;
      left:0px;
    }
    #videoFrame{
      width: 100%;
      height: 600px;
      text-align: center;
      margin:0 auto;
    }
    </style>
  </head>

  <body>
    <header class="navbar navbar-default navbar-fixed-top" role="banner">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">万门大学</a>
        </div>
        <nav class="collapse navbar-collapse" role="navigation">
          <ul class="nav navbar-nav">
            <li class="active">
              <a class="dropdown-toggle" data-toggle="dropdown" href="books.html">知识库<b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><a href="/books.html">图书</a></li>
                <li><a href="/videos.html">视频</a></li>
                <li><a href="/articles.html">经验贴</a></li>
                <li class="divider"></li>
                <li class="dropdown-header">其它分类</li>
                <li><a href="/tags.html">标签</a></li>
                <li><a href="/users.html">有意思的人</a></li>
              </ul>
            </li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
          <li>
            <form method="get" action="/search" class="navbar-form pull-left">
                <input type="text" name="title" placeholder="图书，视频，经验贴" class="span2">
                <button type="submit" class="btn">搜索</button>
            </form>
          </li>
           <% if user_signed_in? %>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown"><img src="<%=current_user[:thumbnail]%>" width="40px"></a>
                  <ul class="dropdown-menu">
                    <li><%= link_to "修改",edit_user_registration_path %> </li>
                    <li> <%= link_to "登出", destroy_user_session_path, :method => :delete %>
                    </li>
                  </ul>
                </li>
            <% else %>
                <li>
                  <a class="navbar-btn btn btn-primary" href="/users/sign_in">登录</a>
                </li>
            <% end %>
          </ul>
        </nav>
      </div>
    </header>
    <% if flash[:alert] %>
    <p class="alert alert-warning">
          <%= flash[:alert] %>
            <button type="button" class="close" data-dismiss="alert">&times;</button>
        </p>
    <% end %>
    <% if flash[:notice] %>
    <p class="alert alert-info">
          <%= flash[:notice] %>
            <button type="button" class="close" data-dismiss="alert">&times;</button>
    </p>
    <% end %>
    <%= yield %>
    <footer class="navbar-default navbar-fixed-bottom" role="contentinfo">
      <div class="container">
        <address class="text-muted">
          &copy; 2013 万门大学 &ensp; 京ICP备12345678号
          <div class="pull-right">
            <a href="about.html">关于万门</a> &bull;
            <a href="join_us.html">加入我们</a> &bull;
            <a href="help.html">帮助中心</a> &bull;
            <a href="terms.html">免责声明</a>
          </div>
        </address>
      </div>
    </footer>

    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="/assets/jquery.js"></script>
    <script src="/assets/jquery.raty.js"></script>
    <script src="/assets/bootstrap.js"></script>
    <script src="/assets/wx-dom.js"></script>
    <script>
    $('#rate_button').hide();
    $('#star').raty({
      score: function() {
        return $(this).attr('data-score');
      },
      click: function(score, evt) {
        $('#rate')[0].value = score;
        $('#rate_button').show();
      }
    });
    function addTag(){
      $('#tagForm').show();
    }
    function hideTag(){
      $('#tagForm').hide();
    }
    function addList(){
      $('#listForm').show();
    }
    function hideList(){
      $('#listForm').hide();
    }
    function showVideo(video){
      if(video.indexOf('youku')!=-1){
        var vid=video.slice(video.indexOf("id_")+3,-5);
        video = "http://player.youku.com/embed/"+vid;
      }
      //window.open(video);
      $('#videoPanel').show();
      $('#videoFrame').attr('src', video);
    }
    function hideVideo(){
      $('#videoPanel').hide();
      $('#videoFrame').attr('src','');
    }
    </script>

  </body>
</html>

